name: Test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  powershell:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7
      - name: Run placement tests
        shell: powershell
        run: .\tests\PipEdgeKeeper.Tests.ps1
